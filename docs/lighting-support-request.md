# Запрос в поддержку KOJIMA

Актуализация: это исторический черновик, сообщение не отправлено. После перехода
в Smart Life привязка к облачному проекту сработала, лампа отображается Online.
После активации IoT Core Trial получен `local_key` и успешно прочитаны восемь DPS
напрямую по LAN (протокол 3.5). Получена облачная карта параметров; три белых
профиля записаны по LAN с точным совпадением при последующем чтении. Визуальная
приёмка ещё ожидается, RGB-команд не отправляли; приложение использует mock.
Причина предыдущего отказа `PARAMS_ILLEGAL` не установлена; исходные наблюдения
ниже сохранены без изменений.

Черновик для отправки владельцем устройства. Сообщение не отправлено.
Контакты: [официальная поддержка](https://kojima.ru/support/), Telegram
[@kojimarus](https://t.me/kojimarus), `service@kojima.ru`.

---

Здравствуйте! Хочу подключить свою лампу KOJIMA E27-RGB-WW-CW-15W к локальному
приложению для управления освещением. Лампа уже привязана к KOJIMA HOME;
в ней настроены пользовательские световые сцены, которые нужно сохранить.

16 сентября 2026 года проверил привязку аккаунта к личному облачному проекту
Tuya Developer Platform через Devices → Link App Account → Add App Account.
KOJIMA HOME считывает QR и предлагает подтвердить вход. После подтверждения
приложение возвращается на главный экран, а сайт показывает QR expired.
Таблица связанных аккаунтов остаётся пустой.

Проверил по очереди Central Europe и Western Europe. В обоих случаях создание
QR проходит успешно, а запрос `/micro-app/cloud/api/v3_3/app/oauth/result`
после подтверждения возвращает:

```json
{
  "success": false,
  "errorCode": "PARAMS_ILLEGAL",
  "status": "error",
  "errorMsg": "Invalid parameters"
}
```

В локальной сети лампа отвечает на обнаружение Tuya и объявляет протокол 3.5.
Локальный ключ пока не получен; команды изменения света из моего приложения
не отправлялись.

Подскажите, пожалуйста:

1. Поддерживается ли такая привязка существующего аккаунта KOJIMA HOME к личному
   проекту Tuya? Какой дата-центр и способ авторизации нужно использовать;
   требуется ли разрешение со стороны KOJIMA?
2. Есть ли поддерживаемый способ получить `local_key` своей лампы либо другой
   программный доступ без сброса и перепривязки устройства?
3. Если требуется переход в Smart Life, поддерживает ли его эта модель и как
   сохранить или экспортировать пользовательские сцены перед переходом?

Спасибо!
